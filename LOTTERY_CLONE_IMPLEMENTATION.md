# 群抽奖和机器人克隆功能实现说明

## 1. 群抽奖功能 (Group Lottery)

### 功能概述
群抽奖功能允许管理员在群组中创建抽奖活动，根据用户在指定时间段内的发言情况进行抽奖。

### 已实现的功能

#### 1.1 消息计数追踪
- **位置**: `app/modules/core/routes.py` 中的 `on_message()` 函数 (lines 7647-7687)
- **功能**: 
  - 自动追踪活跃抽奖期间的用户发言
  - 在 `LotteryMessageCount` 表中记录每个用户的消息数
  - 支持 `message_count` 和 `message_rank` 两种抽奖类型
  - 性能优化：限制同时追踪最多10个活跃抽奖

#### 1.2 自动开奖后台任务
- **位置**: `app/modules/core/routes.py` 中的 `run_lottery_draws()` 函数 (lines 4596-4697)
- **功能**:
  - 每5分钟检查一次已结束但未开奖的抽奖
  - 根据抽奖类型执行开奖：
    - `message_count`: 按消息数加权随机抽取获奖者
    - `message_rank`: 选取发言数最多的前N名用户
  - 更新获奖者ID列表并发送通知到群组
  - 使用HTML格式发送带用户链接的中奖通知

#### 1.3 手动开奖命令
- **命令**: `/lottery_draw <lottery_id>` 或 `/draw <lottery_id>`
- **位置**: `cmd_lottery_draw()` 函数 (lines 6448-6576)
- **权限**: 仅群组管理员可用
- **功能**: 立即执行指定抽奖的开奖逻辑

#### 1.4 查看活跃抽奖命令
- **命令**: `/lottery` 或 `/lottery_list`
- **位置**: `cmd_lottery()` 函数 (新增)
- **功能**: 
  - 显示当前群组的所有活跃抽奖
  - 显示抽奖名称、类型、奖品、结束时间
  - 显示当前参与人数

#### 1.5 查看抽奖历史命令
- **命令**: `/lottery_history`
- **位置**: `cmd_lottery_history()` 函数 (lines 6579-6633)
- **功能**: 
  - 显示最近10条已结束的抽奖
  - 显示获奖者信息（最多显示前5位）

### 数据模型
- `GroupLottery`: 存储抽奖基本信息
- `LotteryMessageCount`: 追踪用户在抽奖期间的发言数

---

## 2. 机器人克隆功能 (Bot Clone)

### 功能概述
机器人克隆功能允许管理员创建和管理多个克隆机器人实例，每个克隆机器人都可以独立运行。

### 已实现的功能

#### 2.1 克隆Bot管理模块
- **位置**: `app/bot_clone_manager.py`
- **功能**:
  - `start_clone_bot()`: 启动一个克隆机器人
  - `stop_clone_bot()`: 停止一个克隆机器人
  - `restart_clone_bot()`: 重启一个克隆机器人
  - `is_clone_running()`: 检查克隆机器人是否运行中
  - `get_active_clone_ids()`: 获取所有运行中的克隆ID
  - `stop_all_clones()`: 停止所有克隆机器人

#### 2.2 克隆Bot启动逻辑
- **位置**: `app/modules/core/routes.py` 中的 `start_all_clone_bots()` 函数
- **功能**:
  - 在主机器人启动后自动启动所有激活的克隆
  - 检查克隆的有效期，跳过已过期的克隆
  - 支持Polling模式（推荐）和Webhook模式

#### 2.3 处理器设置
- **位置**: `setup_clone_handlers()` 函数
- **功能**:
  - 为克隆机器人设置完整的命令和消息处理器
  - 复用主机器人的所有处理器逻辑
  - 每个克隆都有独立的Application实例

#### 2.4 动态启停管理
- **API接口**:
  - `/api/save_bot_clone`: 保存克隆配置，如果克隆正在运行则自动重启
  - `/api/delete_bot_clone`: 删除克隆，自动停止运行中的实例
  - `/api/toggle_bot_clone`: 切换克隆状态，动态启动或停止

#### 2.5 运行模式
- **Polling模式**（推荐）:
  - 克隆机器人主动拉取更新
  - 无需额外的网络配置
  - 适合大多数部署场景

- **Webhook模式**:
  - 需要配置webhook_url
  - 需要反向代理或路由配置将请求转发到正确的处理器
  - 适合高负载场景

### 数据模型
- `BotClone`: 存储克隆机器人的配置
  - `clone_name`: 克隆名称
  - `bot_token`: Bot Token
  - `owner_user_id`: 拥有者用户ID
  - `admin_user_ids`: 管理员用户ID列表（JSON格式）
  - `is_active`: 是否启用
  - `expiration_date`: 有效期
  - `webhook_url`: Webhook URL（可选）

### 使用流程

1. **创建克隆**:
   - 在后台管理界面 `/bot_clones` 创建新克隆
   - 填写克隆名称、Bot Token等信息
   - 设置为激活状态

2. **自动启动**:
   - 主机器人启动时会自动启动所有激活的克隆
   - 或通过切换开关手动启动

3. **管理克隆**:
   - 使用切换开关动态启停克隆
   - 编辑克隆配置时，正在运行的克隆会自动重启
   - 删除克隆时会自动停止运行中的实例

---

## 技术要点

### 北京时区处理
- 使用 `get_beijing_now()` 函数获取北京时间
- 所有时间比较都使用北京时区

### 性能优化
- 抽奖消息计数：限制同时追踪的抽奖数量
- 批量提交数据库更新
- 限制查询结果数量以避免内存问题

### 错误处理
- 克隆Bot Token无效时记录日志并跳过
- 开奖失败时不影响其他抽奖
- API操作失败时返回详细错误信息

### 异步处理
- 使用 `asyncio.run_coroutine_threadsafe()` 在Flask同步上下文中调用异步函数
- 所有克隆操作都在bot的事件循环中执行

---

## 注意事项

1. **克隆机器人权限**:
   - 目前克隆机器人使用与主机器人相同的处理器
   - 未来可以添加权限验证，限制克隆只响应特定用户或群组

2. **Webhook模式限制**:
   - 克隆使用Webhook模式需要额外的路由配置
   - 推荐在简单部署中使用Polling模式

3. **资源管理**:
   - 每个克隆都是独立的Application实例
   - 大量克隆可能会占用较多内存和网络资源

4. **有效期管理**:
   - 已过期的克隆不会自动启动
   - 管理员需要手动更新有效期或删除过期克隆
