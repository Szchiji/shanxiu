# /start 消息功能使用指南

## 功能概述

自定义 /start 消息功能允许您为群组配置个性化的欢迎消息，当用户在群组中使用 `/start` 命令时，机器人会发送您预设的消息。

## 重要提示

⚠️ **Telegram 群组限制**：默认情况下，Telegram 机器人在群组中可能无法接收 `/start` 命令，除非：

1. **关闭隐私模式**：使用 [@BotFather](https://t.me/botfather) 关闭机器人的隐私模式
   - 发送 `/mybots` 给 BotFather
   - 选择您的机器人
   - 选择 `Bot Settings` → `Group Privacy`
   - 选择 `Turn off` 关闭隐私模式

2. **使用 @mention**：用户可以使用 `/start@你的机器人名` 来触发命令

3. **将机器人设为管理员**：某些情况下，将机器人设为群管理员可以帮助接收所有命令

## 功能特性

### 1. 消息类型

- **普通用户消息**：普通群成员使用 `/start` 时显示的消息
- **管理员消息**：管理员使用 `/start` 时显示的特殊消息

### 2. 媒体类型支持

- **文本**：纯文本消息，支持 HTML 格式
- **图片**：发送图片并可附带文字说明
- **视频**：发送视频并可附带文字说明

### 3. 按钮功能

- 支持添加多个内联按钮
- 按钮可以链接到网页
- 支持按钮分行显示
- 自动集成群底部按钮

## 使用步骤

### 步骤 1：进入管理界面

1. 登录机器人后台管理系统
2. 选择您要配置的群组
3. 点击左侧菜单的 "自动回复与消息" → "/start 消息"

### 步骤 2：创建 /start 消息

1. 点击右上角的 "添加消息" 按钮
2. 选择消息类型（普通用户 / 管理员）
3. 选择媒体类型（文本 / 图片 / 视频）

### 步骤 3：编辑消息内容

#### 文本消息

- 在富文本编辑器中输入消息内容
- 支持 HTML 格式：
  - **粗体**：使用 `<b>文字</b>` 或工具栏加粗按钮
  - *斜体*：使用 `<i>文字</i>` 或工具栏斜体按钮
  - 链接：使用 `<a href="url">文字</a>` 或工具栏链接按钮

#### 图片/视频消息

- 输入媒体文件的 URL 地址
- 可选：添加图片/视频说明文字

### 步骤 4：添加按钮（可选）

1. 点击 "添加按钮" 
2. 填写按钮信息：
   - **按钮文字**：显示在按钮上的文字
   - **链接地址**：点击按钮后跳转的 URL
   - **行号**：控制按钮所在行（相同行号的按钮会并排显示）
   - **列顺序**：控制同一行内按钮的排列顺序

### 步骤 5：保存并测试

1. 点击 "保存" 按钮
2. 在群组中发送 `/start` 或 `/start@你的机器人名` 测试效果

## 开关控制

在 "功能配置" 页面中，您可以通过 `start_msg_open` 选项来启用或禁用 /start 消息功能：

- **启用**：机器人会响应 /start 命令并发送自定义消息
- **禁用**：机器人不会在群组中响应 /start 命令

## 示例配置

### 示例 1：简单文本欢迎消息

```
消息类型：普通用户
媒体类型：文本
内容：
👋 欢迎来到我们的群组！

这里是一个友好的交流社区。
请阅读群规并遵守相关规定。

祝您在这里度过愉快的时光！
```

### 示例 2：带按钮的图片消息

```
消息类型：普通用户
媒体类型：图片
图片链接：https://example.com/welcome.jpg
说明文字：
🎉 欢迎加入！
点击下方按钮了解更多信息

按钮配置：
- 按钮1: "📖 查看群规" → https://example.com/rules （行号: 0, 顺序: 0）
- 按钮2: "❓ 常见问题" → https://example.com/faq （行号: 0, 顺序: 1）
- 按钮3: "💬 联系管理员" → https://t.me/admin （行号: 1, 顺序: 0）
```

### 示例 3：管理员专用消息

```
消息类型：管理员
媒体类型：文本
内容：
👮‍♂️ <b>管理员面板</b>

欢迎，管理员！
您可以使用以下命令管理群组：

/kick - 踢出成员
/ban - 封禁成员
/mute - 禁言成员
/menu - 显示菜单

更多功能请访问网页后台。
```

## 常见问题

### Q1: 为什么机器人不响应 /start 命令？

**A**: 可能的原因：
1. 机器人的隐私模式已启用（需要在 BotFather 中关闭）
2. /start 消息功能已被禁用（在功能配置中检查 `start_msg_open`）
3. 没有创建活跃的 /start 消息配置
4. 用户没有使用正确的命令格式（应该使用 `/start@机器人名`）

### Q2: 普通用户和管理员消息有什么区别？

**A**: 
- **普通用户消息**：所有群成员使用 `/start` 时都会看到
- **管理员消息**：只有配置为管理员的用户（在环境变量 ADMIN_ID 中设置）使用 `/start` 时才会看到

管理员消息可以用来提供特殊的管理功能说明或后台入口。

### Q3: 如何让按钮并排显示？

**A**: 设置相同的"行号"即可。例如：
- 按钮A：行号 0
- 按钮B：行号 0
- 按钮C：行号 1

这样A和B会并排显示在第一行，C单独显示在第二行。

### Q4: 可以使用哪些 HTML 标签？

**A**: Telegram 支持以下 HTML 标签：
- `<b>` 或 `<strong>` - 粗体
- `<i>` 或 `<em>` - 斜体
- `<u>` - 下划线
- `<s>` 或 `<strike>` 或 `<del>` - 删除线
- `<code>` - 等宽字体
- `<pre>` - 预格式化文本
- `<a href="url">` - 链接

### Q5: 图片和视频链接有什么要求？

**A**: 
- 必须是公开可访问的 URL
- 推荐使用 HTTPS 协议
- 图片支持格式：JPG, PNG, GIF
- 视频支持格式：MP4
- 文件大小限制：图片 5MB，视频 20MB

## 技术细节

### 数据库表结构

`start_messages` 表包含以下字段：
- `id` - 主键
- `group_id` - 关联的群组 ID
- `message_type` - 消息类型（'user' 或 'admin'）
- `media_type` - 媒体类型（'text', 'image', 'video'）
- `media_url` - 媒体文件 URL
- `content` - 消息内容（支持 HTML）
- `links` - 按钮配置（JSON 格式）
- `is_active` - 是否启用
- `created_at` - 创建时间
- `updated_at` - 更新时间

### API 端点

- `POST /core/api/save_start_message` - 保存或更新消息
- `POST /core/api/toggle_start_message` - 切换消息启用状态
- `POST /core/api/delete_start_message` - 删除消息

## 最佳实践

1. **保持简洁**：欢迎消息不要太长，重点突出关键信息
2. **使用按钮**：将详细信息放在外部链接中，通过按钮引导用户
3. **定期更新**：根据群组活动和规则变化及时更新欢迎消息
4. **测试效果**：配置完成后一定要在群组中测试实际效果
5. **分别配置**：为普通用户和管理员配置不同的消息内容

## 相关功能

- **自动回复**：为特定关键词配置自动回复消息
- **定时消息**：定时向群组发送消息
- **群底部按钮**：为群组添加常驻底部按钮菜单

## 更新日志

- **2024-01**: 初始实现，支持文本、图片、视频类型
- **2024-01**: 添加按钮支持
- **2024-01**: 集成群底部按钮功能
- **2024-01**: 改进空内容处理逻辑

## 支持

如有问题或建议，请联系开发者或在 GitHub 提交 Issue。
