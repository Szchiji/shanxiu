# 测试说明 - 禁言与续期修复

本文档说明如何测试已实施的修复。

## 已修复的问题

1. ✅ 机器人权限不足导致禁言/解禁失败 (Chat_admin_required)
2. ✅ JobQueue 启动警告 (`RuntimeWarning: coroutine 'JobQueue.start' was never awaited`)
3. ✅ 解禁失败时不当的回滚逻辑（不再回滚到期时间）
4. ✅ 增强的日志记录

## 测试场景

### 场景 1: 机器人无权限的情况

**测试步骤:**
1. 在一个测试群组中，暂时移除机器人的"限制成员"权限
2. 尝试通过后台管理续费一个已过期的用户
3. 检查日志输出

**预期结果:**
- 日志会显示: `⚠️ 机器人在群组 [群组名] (ID: xxx) 中缺少 'can_restrict_members' 权限`
- 系统返回错误信息，提示管理员授予权限
- **重要**: 用户的到期时间已被更新，但保持禁言状态
- 管理员授予权限后，可以再次操作解除禁言

**验证日志关键字:**
```
🔍 [api_save_user] 检查机器人权限
⚠️ 机器人在群组 ... 中缺少 'can_restrict_members' 权限
✅ [api_save_user] 已更新用户 ... 的到期时间
❌ [api_save_user] ... 但解除禁言失败
```

### 场景 2: 定时任务检查过期用户

**测试步骤:**
1. 创建一个测试用户，设置到期时间为过去
2. 等待定时任务执行（默认每小时执行一次，或重启机器人立即触发）
3. 检查日志输出

**预期结果:**
- 日志会显示权限检查过程
- 对于有权限的群组，成功禁言
- 对于无权限的群组，跳过并回滚 `is_banned` 状态

**验证日志关键字:**
```
🕐 [check_expired_users] 任务开始执行
🔍 [check_expired_users] 找到 X 个过期用户需要禁言
✅ 机器人在群组 ... 中拥有禁言权限
🔨 [check_expired_users] 开始禁言用户
⛔️ [check_expired_users] 成功禁言用户
📊 [check_expired_users] 任务完成 - 成功: X, 失败: Y
```

### 场景 3: JobQueue 启动验证

**测试步骤:**
1. 在 Webhook 模式下启动机器人
2. 检查启动日志

**预期结果:**
- 不应出现 `RuntimeWarning: coroutine 'JobQueue.start' was never awaited`
- 日志显示: `✅ Job queue 已正确启动（使用 await）`

**验证日志关键字:**
```
✅ Job queue 已正确启动（使用 await）
✅ Bot 初始化完成 (Webhook 模式)
```

### 场景 4: 续费操作（有权限）

**测试步骤:**
1. 确保机器人在群组中有管理员权限和"限制成员"权限
2. 为一个已禁言的过期用户续费
3. 检查日志和用户状态

**预期结果:**
- 权限检查通过
- 到期时间更新
- 成功解除禁言
- 用户可以正常发言

**验证日志关键字:**
```
✅ [api_save_user] 已更新用户 ... 的到期时间
🔍 [api_save_user] 检查机器人权限
✅ 机器人在群组 ... 中拥有禁言权限
🔓 [api_save_user] 尝试在 Telegram 解除禁言
✅ [api_save_user] 成功解除用户 ... 的禁言
```

### 场景 5: 续费操作（无权限）

**测试步骤:**
1. 移除机器人的"限制成员"权限
2. 为一个已禁言的过期用户续费
3. 检查日志和数据库

**预期结果:**
- 权限检查失败
- **到期时间已更新**（这是修复的关键点）
- 解除禁言失败，但不回滚到期时间
- 返回清晰的错误信息

**验证日志关键字:**
```
✅ [api_save_user] 已更新用户 ... 的到期时间
🔍 [api_save_user] 检查机器人权限
❌ [api_save_user] ... 但解除禁言失败
⚠️ [api_save_user] 到期时间已更新为 ...，但解除禁言失败
⚠️ [api_save_user] 用户保持禁言状态，请手动在 Telegram 中解除或检查机器人权限
```

## 检查日志的方法

### 方法 1: 实时日志（推荐）
```bash
# 如果使用 Railway 或其他云平台
# 在平台的日志查看器中查看实时日志

# 如果本地运行
python run.py
```

### 方法 2: 搜索关键日志
```bash
# 搜索权限相关日志
grep "check_bot_restrict_permissions\|机器人权限" logs.txt

# 搜索 JobQueue 启动日志
grep "Job queue\|JobQueue" logs.txt

# 搜索续费操作日志
grep "api_save_user" logs.txt

# 搜索过期用户检查日志
grep "check_expired_users" logs.txt
```

## 回滚逻辑验证

**修复前的问题:**
- 解禁失败时，系统会同时回滚 `expiration_date` 和 `is_banned`
- 用户续费记录丢失

**修复后的行为:**
- 先更新并提交 `expiration_date`
- 再尝试解除禁言
- 如果解禁失败，`expiration_date` 保持更新后的值
- 只有 `is_banned` 保持为 `True`
- 管理员修复权限后可以重新操作解除禁言

**验证方法:**
1. 在数据库中查询测试用户的记录
2. 在无权限情况下续费
3. 检查 `expiration_date` 是否已更新
4. 检查 `is_banned` 是否仍为 `True`

```sql
-- 查询用户状态
SELECT tg_id, expiration_date, is_banned 
FROM group_users 
WHERE tg_id = [测试用户ID];
```

## 常见问题排查

### Q: 日志中没有权限检查信息
**A:** 检查是否在正确的操作场景（续费或定时任务）中触发了相关代码

### Q: JobQueue 警告仍然出现
**A:** 确保代码已更新到最新版本，检查 `run_bot()` 函数中的 `await app.job_queue.start()` 行

### Q: 续费后用户仍然被禁言
**A:** 
1. 检查机器人是否有管理员权限
2. 检查机器人是否有"限制成员"权限
3. 查看日志中的权限检查结果
4. 如果权限没问题，检查用户的到期时间是否确实被更新

## 成功指标

修复成功后，应该观察到:

1. ✅ 不再出现 `Chat_admin_required` 错误（在有权限的群组中）
2. ✅ 对于无权限群组，有清晰的错误提示
3. ✅ JobQueue 启动无警告
4. ✅ 续费操作不会因为解禁失败而回滚到期时间
5. ✅ 详细的日志帮助快速定位问题

## 技术支持

如果遇到问题:
1. 收集相关日志（权限检查、续费操作、定时任务日志）
2. 检查机器人在群组中的权限设置
3. 验证数据库中用户记录的状态
4. 在 GitHub Issue 中提供详细的错误信息和日志
